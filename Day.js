class Day {
    constructor(matches) {
        this.#matches = matches;
    }

    get matches() {
        return this.matches;
    }
    set matches(matches) {
        this.matches = matches;
    }
}